% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duplicate-groups.R
\name{find_duplicate_groups}
\alias{find_duplicate_groups}
\title{Group near-duplicate images by hash distance}
\usage{
find_duplicate_groups(hash_df, threshold = 5)
}
\arguments{
\item{hash_df}{A data frame with at least columns \code{path} and \code{hash},
as returned by \code{\link[=compute_hashes]{compute_hashes()}}.}

\item{threshold}{Maximum Hamming distance (in bits, out of 64) for two
images to be considered near-duplicates. Defaults to \code{5}, a
reasonably conservative value for dHash; lower it for stricter
(exact-duplicate-only) matching, raise it to catch more aggressive
near-duplicates at the cost of more false positives.}
}
\value{
\code{hash_df} with an added integer column \code{group_id}. Images with
no near-duplicates get their own unique \code{group_id}. Row order is
preserved.
}
\description{
Given a table of image hashes (as produced by \code{\link[=compute_hashes]{compute_hashes()}}),
clusters images into duplicate groups: any two images within
\code{threshold} Hamming distance of each other are placed in the same
group, and group membership propagates transitively (A~B~C all end
up in one group even if A and C aren't directly close).
}
\examples{
hashes <- data.frame(
  path = c("a.jpg", "b.jpg", "c.jpg", "d.jpg"),
  hash = c("0000000000000000", "0000000000000001",
           "ffffffffffffffff", "1234567890abcdef")
)
find_duplicate_groups(hashes, threshold = 5)

}
