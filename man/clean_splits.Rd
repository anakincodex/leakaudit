% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean-splits.R
\name{clean_splits}
\alias{clean_splits}
\title{Produce a leak-free split assignment}
\usage{
clean_splits(grouped_df, priority = c("train", "val", "test"))
}
\arguments{
\item{grouped_df}{A data frame with columns \code{path}, \code{group_id}, and
\code{split}, as produced by \code{\link[=find_duplicate_groups]{find_duplicate_groups()}}.}

\item{priority}{Character vector giving split precedence, highest
priority first. Defaults to \code{c("train", "val", "test")}: a leaked
group touching train and test is fully reassigned to train.}
}
\value{
\code{grouped_df} with \code{split} replaced by the corrected,
leak-free assignment, plus a logical column \code{reassigned} flagging
which rows were changed.
}
\description{
Resolves duplicate groups that span multiple splits by assigning every
member of a leaked group to a single split, chosen by \code{priority}. This
mirrors the common convention of keeping duplicates in \code{train} (so no
information is discarded, only potential evaluation leakage) but any
priority order can be supplied.
}
\examples{
groups <- data.frame(
  path = c("a.jpg", "b.jpg", "c.jpg"),
  split = c("train", "test", "val"),
  group_id = c(1, 1, 2)
)
clean_splits(groups)

}
