% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dhash-audit.R
\name{dhash_audit}
\alias{dhash_audit}
\title{Audit train/val/test leakage from duplicate groups}
\usage{
dhash_audit(grouped_df)
}
\arguments{
\item{grouped_df}{A data frame with columns \code{path}, \code{group_id}, and
\code{split}, as produced by \code{\link[=find_duplicate_groups]{find_duplicate_groups()}} (with \code{split}
supplied to \code{\link[=compute_hashes]{compute_hashes()}}).}
}
\value{
An object of class \code{leakage_report} (a list) with:
\describe{
\item{leaked_groups}{data frame of group_ids that span >1 split,
with the splits involved and member count}
\item{n_leaked_images}{total images sitting in a leaked group}
\item{pct_leaked_by_split}{named numeric vector, \% of each split's
images that belong to a leaked group}
\item{total_images}{total images audited}
}
}
\description{
Given duplicate groups (from \code{\link[=find_duplicate_groups]{find_duplicate_groups()}}) and each
image's split assignment, reports how many groups span more than one
split -- i.e. how many near-duplicate images "leak" across the
train/validation/test boundary -- and what fraction of each split is
affected.
}
\examples{
groups <- data.frame(
  path = c("a.jpg", "b.jpg", "c.jpg", "d.jpg"),
  split = c("train", "test", "train", "val"),
  group_id = c(1, 1, 2, 3)
)
dhash_audit(groups)

}
